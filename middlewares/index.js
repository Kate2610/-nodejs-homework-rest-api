const checkId = require('./checkId');
const handleMongooseError = require('./handleMongooseError');
const auth = require('./auth');
const upload = require('./upload');
module.exports = { checkId, handleMongooseError, auth, upload };